你现在的任务是：根据我提供的一个网站 URL，自主浏览该网站，识别对网站复刻最有价值的页面，并保存必要的参考页面与结果。

我只会提供一个 URL，例如：

```text
https://www.everlane.com
```

不要让我手动列页面，也不要一开始给我计划后等待确认。直接开始工作，自己判断并完成。

目标不是爬完整个网站，也不是保存所有页面，而是用最少数量的页面覆盖尽可能多的页面模板、布局、交互和视觉模式，最终产出一套适合后续由 Codex / AI 复刻 Shopify 网站的 reference。

工作原则：

1. 先从首页开始浏览整个网站的信息架构。
2. 检查主导航、mega menu、footer、站内链接和重要内容入口。
3. 主动判断有哪些不同类型的页面模板。
4. 对结构明显重复的页面不要重复保存。
5. 每种重要模板只选择最有代表性的 1～3 个页面。
6. 优先选择信息最完整、交互最多、视觉结构最典型的页面。
7. 不要为了“覆盖数量”去保存大量相同 Product 或 Collection 页面。
8. 不需要登录，不处理 checkout，不执行购买，不提交表单，不绕过验证码或访问限制。
9. 遇到 429、验证码或访问限制时跳过该页面，继续处理其他页面。

至少需要判断以下页面类型是否存在：

```text
Home
Product
Collection / Category
Search
About / Company
FAQ
Shipping
Returns
Contact / Help
Stores
Blog / Article
Landing Page
Editorial / Campaign Page
Other special templates
```

对于 Product 页面，不要只随机选择商品。

优先选择可以代表不同产品模板或复杂度的页面，例如：

```text
多颜色
多尺寸
多个 variant
多图
视频
accordion
sticky add-to-cart
特殊 merchandising
不同 gallery 布局
不同产品信息结构
```

如果多个 Product 页面结构完全相同，只保存少量代表页面。

对于 Collection 页面：

优先选择：

```text
有筛选
有排序
不同商品卡片布局
不同 banner / hero
不同 merchandising section
不同分页或无限滚动形式
```

如果结构相同，不重复保存。

浏览过程中，主动检查并记录重要交互：

```text
header / mega menu
mobile navigation
dropdown
accordion
tabs
carousel
modal
product gallery
variant selector
filter
sort
sticky elements
announcement bar
newsletter popup
cookie banner
footer sections
```

对于需要交互后才能看清结构的内容，可以安全地点击展开。

允许操作：

```text
展开菜单
展开 accordion
切换 tab
切换图片
切换非购买性质的 variant
滚动页面
关闭 cookie banner
关闭 newsletter popup
切换 desktop / mobile viewport
```

不要执行：

```text
Add to cart
Checkout
登录
注册
发送表单
订阅
支付
任何不可逆操作
```

每个被选中的页面都需要尽量保存以下参考：

```text
完整页面归档
Desktop 页面截图
Mobile 页面截图
原始 URL
页面类型
页面标题
关键页面结构说明
特殊交互说明
```

如果当前环境支持 Safari：

优先使用 Safari WebArchive 保存完整页面。

如果使用 Chrome / Edge：

优先使用 SingleFile 保存单 HTML。

如果这些方式不可用，则至少确保保存：

```text
HTML
Desktop full-page screenshot
Mobile full-page screenshot
```

文件组织尽量保持清晰，例如：

```text
references/
  home/
  product/
  collection/
  about/
  faq/
  shipping/
  returns/
  stores/
  blog/
  special/
```

文件名必须有意义，避免全部使用 page1、page2 这样的名字。

例如：

```text
product-womens-day-glove
collection-womens-shoes
about-company
returns-policy
```

同时生成一个总的页面清单。

格式例如：

```text
site-reference-summary.md
```

内容至少包括：

```text
站点 URL
发现的主要页面模板
最终保存了哪些页面
每个页面为什么值得保留
哪些页面结构重复因此被跳过
主要导航结构
header / footer 结构
主要视觉风格
主要字体和 spacing 特征
主要 UI pattern
主要 Product 模板
主要 Collection 模板
重要交互
Desktop / Mobile 的主要差异
```

另外生成一个机器可读文件：

```text
manifest.json
```

例如：

```json
{
  "site": "https://example.com",
  "pages": [
    {
      "type": "product",
      "title": "Example Product",
      "url": "https://example.com/products/example",
      "file": "references/product/example-product",
      "reason": "Representative PDP with color, size, gallery and accordion"
    }
  ]
}
```

你需要自己控制采集规模。

默认目标：

```text
10～25 个页面
```

如果 10 个页面已经足够覆盖所有模板，不要为了达到数量继续保存重复页面。

如果网站结构非常复杂，可以超过 25 个，但必须说明为什么。

最终重点不是“抓了多少页面”，而是：

```text
模板覆盖率高
重复页面少
对后续复刻有帮助
Desktop / Mobile 都有参考
重要交互没有遗漏
```

完成后自行检查：

1. 是否有首页。
2. 是否至少有一个 Product。
3. 是否至少有一个 Collection。
4. 是否覆盖主要静态内容模板。
5. 是否保存了 header/footer 的代表页面。
6. 是否观察了 mobile 版本。
7. 是否遗漏明显不同的模板。
8. 是否保存了明显重复的页面。
9. 文件是否可以正常打开。
10. manifest 与实际文件是否对应。

如果发现遗漏，继续补充，不要先询问我。

最终只需要向我汇报：

```text
共发现多少种页面模板
实际保存多少个页面
保存了哪些页面
哪些模板被认为是重复的
哪些页面因为限制没有成功保存
reference 文件保存在哪里
site-reference-summary.md 在哪里
manifest.json 在哪里
```

不要只给分析或建议。

直接使用浏览器 / Computer Use 实际完成网站浏览、页面筛选、保存和验证。